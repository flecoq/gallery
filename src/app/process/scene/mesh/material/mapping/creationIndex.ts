
export class CreationIndex {
	
	public list: number = 0;
	public layer: string;
	public size: string;
	
	constructor(list: number, layer: string, size: string) {
		this.list = list;
		this.layer = layer;
		this.size = size;
	}
}